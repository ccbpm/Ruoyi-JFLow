## PanelGroup 使用说明

- 多用于两个实体之间的关系数据维护，比如:模块菜单\表单类别表单.
- 需要两个数据源. 一个是分组实体，一个是数据源实体.
- 需要设置关联的RefKey 把两个数据实体关联起来.
- 多用一为维护.
- 分组下，可以数据为空,但是GL的分组数据是从一个数据源上求出来的.
- 一个实体，可以从一个分组移动到另外一个分组，不需要写特别的代码。
- 分组移动顺序，实体之间移动顺序也不需要写代码.

### 系统约定

- IsEanble 如果实体中有这个字段就是是否启用.
- 
