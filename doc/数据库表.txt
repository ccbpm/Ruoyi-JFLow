-- 组织结构数据库.

SELECT * FROM Port_Emp; --用户
SELECT * FROM Port_Dept; --部门.
SELECT * FROM Port_DeptEmp; --部门人员对应.
SELECT * FROM Port_DeptEmpStation; --部门人员岗位.
SELECT * FROM Port_StationType; --岗位类型。
SELECT * FROM Port_Station; -- 岗位.
SELECT * FROM Port_Org; -- 组织.
SELECT * FROM Port_OrgAdminer; --管理员.

-- 流程模版.

SELECT * FROM WF_Flow WHERE No='037';  -- 流程属性. 
SELECT * FROM WF_Node WHERE FK_Flow='037'; -- 节点属性.
SELECT * FROM WF_Direction WHERE FK_Flow='037'; -- 连接线.
SELECT * FROM WF_LabNote  WHERE FK_Flow='037'; -- 备注信息. 
SELECT * FROM WF_Cond  WHERE FK_Flow='037'; -- 方向条件.

-- 流程数据表.
SELECT * FROM ND37Rpt  WHERE OID=286;  -- 流程数据表. 表自动生成.
SELECT * FROM ND37Track WHERE WorkID=286 ORDER BY RDT; -- 轨迹表.

-- 流程引擎表.
SELECT * FROM WF_GenerWorkFlow WHERE  FK_Flow='037' and WorkID=286; -- 流程引擎注册表, 主键WorkID
SELECT * FROM WF_GenerWorkerlist WHERE WorkID=286; -- 工作人员表.主键 WorkID,FK_Emp,FK_Node.
SELECT * FROM Sys_Enum WHERE EnumKey='WFState' -- 流程状态枚举.4、

@admin,admin@guoxiangbin,郭祥斌@

SELECT * FROM WF_GenerWorkFlow WHERE TODOEMPS LIKE '%@zhangsan%' and WFState=3 and fk_flow='037'

SELECT * FROM WF_GenerWorkFlow WHERE TODOEMPS LIKE '%@zhangsan%' and WFState!=3 and todoemps like '%@zhangwsna%' and fk_flow='037'


select * from sys_enum where EnumKey='WFState'

-- 求待办的视图

SELECT A.WorkID, A.Title, a.FK_Flow, a.FK_Node , a.NodeName , b.FK_Emp
 FROM WF_GenerWorkFlow A, WF_GenerWorkerlist B 
WHERE A.WorkID=B.WorkID AND A.FK_Node=B.FK_Node AND B.IsPass=0 

-- 求在途的视图

SELECT a.Title, A.WorkID, a.FK_Node,a.NodeName FROM WF_GenerWorkFlow A, WF_GenerWorkerlist B 
WHERE A.WorkID=B.WorkID and B.FK_Emp='liping' and B.IsPass=1

-- 综合查询SQL

select * from WF_GenerWorkFlow where fk_dept='1001' --综合查询.
select * from WF_GenerWorkFlow where Starter='zhangsan' --查询zhangsan发起的流程.

-- 表单模版

SELECT * FROM Sys_MapData WHERE No='ND1001';  --模版注册表.
SELECT * FROM Sys_MapAttr WHERE FK_MapData='ND1001'; -- 字段表.
SELECT * FROM Sys_MapDtl WHERE FK_MapData='ND1001'; --从(子)表
SELECT * FROM Sys_MapAttr WHER


 外键表属性   Sys_SFTable   联动的数据 Sys_FrmRB